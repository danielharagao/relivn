{
  "name": "relivn",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "test": "node --test tests/*.test.js",
    "test:e2e": "node scripts/e2e.js",
    "check": "npm test && node scripts/check-static.js && npm run test:e2e",
    "serve": "python3 -m http.server 4173"
  },
  "devDependencies": {
    "puppeteer-core": "^24.16.0"
  }
}
